#include <bits/stdc++.h>
using namespace std;

/*
Count the number of integers between a,b where no two digits are the same. This is certainly given the number
of values where the digits ARE the same. This is relate

*/